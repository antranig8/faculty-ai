# FacultyAI Frontend

Next.js app for the FacultyAI live feedback demo.

## Development

```powershell
npm install
npm run dev
```

Open `http://localhost:3000/present`.

Set `NEXT_PUBLIC_API_BASE_URL` if the backend is not running on `http://localhost:8000`.
